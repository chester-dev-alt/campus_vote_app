import 'package:flutter/material.dart';

class ActiveElections extends StatelessWidget {
  const ActiveElections({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Voting')),
      body: const Center(child: Text('Voting Screen - Coming Soon')),
    );
  }
}