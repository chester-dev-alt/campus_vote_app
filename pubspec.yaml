name: campus_vote_app
description: Campus Governance and Voting Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # ← FIX THIS LINE

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.6

  # Firebase
  firebase_core: 3.6.0
  firebase_auth: 5.3.1
  cloud_firestore: 5.4.4

  # Other packages
  go_router: 14.2.7
  provider: 6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true